import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import { SpellBeeSolver } from './SpellBeeSolver';

ReactDOM.render(
  <React.StrictMode>
    <SpellBeeSolver />
  </React.StrictMode>,
  document.getElementById('root')
);